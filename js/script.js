console.log("Ukraine Travel Website Loaded");

// Add simple hover effect logic/animations if needed
document.querySelectorAll('.nav-btn').forEach(btn => {
    btn.addEventListener('mouseenter', () => {
        // Optional JS effects
    });
});
